/**
 * Bootstrap entry so "npm start" always runs from repo root.
 * Run: node start.js (from directory where package.json lives).
 * This loads src/index.js; all imports then resolve correctly (no src/src).
 */
import "./src/index.js";
